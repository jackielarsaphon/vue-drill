services:
  - type: web
    name: vue-drill
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l $PORT
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
